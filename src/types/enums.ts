export enum View {
  GARAGE = 'garage',
  WINNERS = 'winners',
}

export enum Path {
  GARAGE = '/garage',
  ENGINE = '/engine',
  WINNERS = '/winners',
}

export enum EngineStatus {
  STARTED = 'started',
  STOPPED = 'stopped',
  DRIVE = 'drive',
}

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum SortType {
  ID = 'id',
  WINS = 'wins',
  TIME = 'time',
}
