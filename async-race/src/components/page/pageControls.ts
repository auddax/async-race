import { IPageControls } from '../../types/interfaces';

class PageControls implements IPageControls {
  // eslint-disable-next-line class-methods-use-this
  render() {
    return '';
  }
}

export default PageControls;
