export enum View {
  GARAGE = 'garage',
  WINNERS = 'winners',
}

export enum Path {
  GARAGE = '/garage',
  ENGINE = '/engine',
  WINNERS = '/winners',
}

export enum EngineStatus {
  STARTED = 'started',
  STOPPED = 'stopped',
  DRIVE = 'drive',
}
